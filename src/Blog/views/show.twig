{%  extends 'layout.twig' %}

{% block title slug %}

{% block body %}

    <h1>{{ post.name }}</h1>
    <p class="text-muted">{{ post.created_at | ago }}</p>
    {{ post.content | nl2br }}

{% endblock %}